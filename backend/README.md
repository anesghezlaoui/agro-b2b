# Backend Django (wrapper)

Le code Django est actuellement situé au niveau racine du projet :

- `manage.py`
- `config/`
- `core/`
- `commerce/`

Ce dossier `backend/` sert uniquement de point d’entrée visuel :

```bash
python backend/manage.py runserver 0.0.0.0:8000
```

